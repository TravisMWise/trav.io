import { Collision } from "../parent_classes/directory_export.js";

export class BoxCollision extends Collision {
    constructor(p, x, y, w, h) {
        super(p, x, y, w, h);
    }

    draw(p) {

    }
}